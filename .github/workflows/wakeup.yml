name: Wake Render Bot

on:
  schedule:
    - cron: "30 10 * * *"   # TEST time
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          echo "Waking up Render bot..."
          curl -I https://bot-server-v3.onrender.com
