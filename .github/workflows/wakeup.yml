name: Wake Render Bot

on:
  schedule:
    - cron: "18 11 * * *"   # TEST time
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          echo "Waking up Render bot..."
          curl -I https://manager-v2-adot.onrender.com
